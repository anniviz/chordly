import styled from 'styled-components/macro'

const SideListTitle = styled.h2`
  padding: 0 10px 10px 10px;
  max-height: 100px;
  overflow-wrap: break-word;
  font-size: 1.2rem;
`
export default SideListTitle
