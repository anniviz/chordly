import React from 'react'
import styled from 'styled-components/macro'
import Button from './Button'

export default function SongListButton() {
  return <Button style={{ gridColumn: 1 / 2 }}>Show</Button>
}
